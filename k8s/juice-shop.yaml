---
# 1. DEPLOYMENT CHO JUICE SHOP
apiVersion: apps/v1
kind: Deployment
metadata:
  name: juice-shop
  namespace: isolated-staging
  labels:
    app: juice-shop
spec:
  replicas: 1
  selector:
    matchLabels:
      app: juice-shop
  template:
    metadata:
      labels:
        app: juice-shop
    spec:
      containers:
      - name: juice-shop
        image: ${app_image} # 🌟 TOKEN ĐỂ CI/CD TỰ ĐỘNG ĐIỀN DOCKER IMAGE
        ports:
        - containerPort: 3000
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
# 2. SERVICE XUẤT PORT 80 CHO DAST SCAN
apiVersion: v1
kind: Service
metadata:
  name: juice-shop-service
  namespace: isolated-staging
spec:
  type: LoadBalancer
  selector:
    app: juice-shop
  ports:
  - protocol: TCP
    port: 80 # Mở port 80 thẳng ra Internet
    targetPort: 3000 # Port thực tế của container
